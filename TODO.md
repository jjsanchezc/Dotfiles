# TODO

Tracks drift between `README.md` (and subdirectory READMEs) and what's actually on the system. Last audited: 2026-08-09.

## 1. README says it's active, but it isn't

Startup Applications table in `README.md` vs. `hypr/hyprland.lua`'s `hl.on("hyprland.start", ...)` block:

- [x] ~~`hypridle`~~ **RESOLVED 2026-08-09.** Wrote a real `hypr/hypridle.conf` (5 listeners: keyboard backlight off 2.5min, screen dim 5min, lock 6min, DPMS off 6.5min, suspend 20min — timeouts tuned by hand after an initial draft) and uncommented the exec line in `hyprland.lua`. Two things adapted from the Hyprland wiki example rather than copied blind: the keyboard backlight device is `asus::kbd_backlight` on this hardware, not the generic `rgb:kbd_backlight`; and `loginctl lock-session` (used for both `before_sleep_cmd` and the lock listener) only works because hypridle itself listens for that logind signal and runs `general.lock_cmd` in response — it was a no-op before since hypridle was never running. Validated with a dry run (`hypridle -c ... -v`, all 5 rules registered, no parse errors) before touching the exec line, then started live (`pidof hypridle` confirms one instance, dbus sleep-inhibitor registered) — no need to wait for next login.
- [x] ~~`power-saver`~~ **RESOLVED 2026-08-06.** In the autostart block, confirmed running live.
- [x] ~~`ags-watch`~~ **RESOLVED.** In the autostart block, AGS confirmed running (`ags list`). Needed a missing `~/.config/ags` symlink, a missing `cd $AGS_DIR`, missing `sass`/`inotify-tools`, and 6 missing `libastal-*-git` AUR packages.
- [x] ~~`kitty` (nvim, workspace 3)~~ **RESOLVED 2026-08-09 — dropped, not restored.** Traced it to the repo's very first commit (`bcd2f09f`) — it was real, not aspirational: `exec-once = [workspace 3 silent] kitty tmux new-session nvim`, alongside the workspace-1 tmux one that did survive into `hyprland.lua`'s `hl.on("hyprland.start", ...)` block. Lost somewhere before the Lua migration, never carried over. User's call: not worth restoring — README row (and every other reference) removed instead.
- [x] ~~`battery-alert.service`/`.timer`~~ **RESOLVED 2026-08-09.** Also found `acpi` (the script's actual battery-reading command) wasn't installed either — installed it (official `extra` repo). System reports two ACPI batteries; the script already filters to `Battery 0` specifically, so `Battery 1` (phantom, stuck at 0%) doesn't false-trigger it — verified by running the script by hand first (clean exit, no notification, correct given battery was at 67% and not discharging/full). Copied both unit files to `~/.config/systemd/user/` (real dir, not symlinked — see `tmux/`-style entries elsewhere in this doc for why that distinction matters), `daemon-reload`, `enable --now`. Confirmed live: `systemctl --user list-timers` shows it scheduled, first run already fired via `OnBootSec=1m` and exited clean (`status=0/SUCCESS`).

Pick one per item: wire it up for real, or trim the README table to match reality.

## 2. Actually missing / broken

- [x] ~~`hyprlock` background hardcoded~~ **RESOLVED 2026-08-06.** `hyprlock.conf.in` now uses a `__WALLPAPER_SET_PATH__` marker `sed`-replaced by `wallpaper-set`, works across automatic and all 3 fixed themes.
- [x] ~~AGS post-hook CWD bug~~ **RESOLVED 2026-08-06.** Redundant/broken post_hook removed from `matugen/config.toml`; `ags-watch` already handles the AGS restart.
- [x] ~~Orphaned `hypr/wallpapers/`~~ **RESOLVED 2026-08-06.** Deleted.
- [x] ~~Theming doesn't cover everything: `starship.toml` has no matugen template~~ **RESOLVED 2026-08-09 — user's call, not worth it.** It's the active prompt but barely styled to begin with (only `success_symbol = '[❯](bold green)'`, a built-in color name, no hex anywhere) — theming it would mean writing most of the prompt's module config from scratch, not just swapping colors, since Starship has no external-include mechanism (the whole file would become matugen-generated, same pattern as `hyprlock.conf.in`). Decided it's not worth the effort for what it'd add. Stays hardcoded.
- [x] ~~`starship.toml` unused, p10k is the real prompt~~ **CORRECTED then RESOLVED 2026-08-09.** This was backwards — `zsh` was never installed, so `.zshrc`/Powerlevel10k were never active; Starship is the real prompt, initialized from `bash/.bashrc`. `zsh/` (`.zshrc`, `.zshenv`, `.zprofile`, `.p10k.zsh`) has now been deleted from the repo entirely, and both READMEs updated to match.
- [x] ~~`foot/`/`mako/` dead config folders~~ **INVESTIGATED 2026-08-06.** Neither installed/symlinked/referenced. `foot/` kept in case it's wanted as an alt terminal later. AGS's own `Notifications.tsx`/`ControlCenter.tsx` are the real, active, matugen-themed notification system — the daemon actually competing for them was `dunst` (not mako, never in this repo), now masked (`systemctl --user mask dunst.service`). `mako/` stays in the repo unused.
- [x] ~~Notification icons~~ **RESOLVED 2026-08-06.** `Notifications.tsx` now prefers the notification's own image over the app icon; handles icon-theme names and file paths.
- [x] ~~Notification `out of tracking context` error + visible closing glitch~~ **RESOLVED 2026-08-09 — two separate bugs, not one.** User spotted a real visible symptom: a thin `$border`-colored sliver left on screen after a popup closed (screenshot notifications reproduced it on demand, every time). Two fixes were needed, not one:
  1. `NotificationPopup` builds its JSX from inside the `notifd.connect("notified", ...)` event callback rather than as a rendered component, so it never had a tracking-context scope — every `onCleanup()` Astal's JSX runtime tried to register for it failed (the `CRITICAL: out of tracking context` log line, on every notification). Fixed by wrapping the JSX-building portion in `createRoot(dispose => {...})` (the same helper Astal uses internally to run `app.ts`'s own `main()` in a scope) and calling the returned `dispose()` alongside the existing manual cleanup.
  2. That alone did **not** fix the visible sliver — confirmed by testing, the line was still there after step 1, log or no log. Real cause: the `notifications` window (unlike `control-center`) had **no** window-resize-reset logic at all — same underlying issue as the ControlCenter bluetooth-ellipsis bug from earlier this session (GTK layer-shell windows don't shrink back down on their own). Added a `resetNotifWindowSize()` (`set_default_size(-1, -1)`, same fix as ControlCenter's) called after a popup finishes removing itself.
  Verified by the user directly after both fixes: sliver confirmed gone. (First reported this fixed after only step 1, based on the log going quiet — that was wrong, log-quiet ≠ visually-fixed; corrected once the user re-tested and caught it still happening.)
- [x] ~~`ControlCenter.tsx` has no live UI trigger~~ **RESOLVED 2026-08-09.** Decision: keep waybar's network/bluetooth icons on `iwgtk`/`blueman-manager` (already functional, just not themed) and give ControlCenter its own dedicated bind instead of reclaiming either icon — `Super+C` → `ags request -i jjsanchezc-shell toggle-cc`, added to `keybindings.lua`. First version of the bind omitted `-i jjsanchezc-shell` (same "instance ags is not runnning" bug as the very first manual test) — the key press ran silently failed with no visible error until reported; fixed and reverified with `hyprctl --verify-config` + a direct run of the exact bound command before reloading live.
- [x] ~~`app.ts`'s `requestHandler` never matched anything~~ **RESOLVED 2026-08-09 — real, previously-unknown bug, not just the trigger being missing.** The `"request"` signal passes the CLI's argv as an array (`(_, args, response) => requestHandler(args, response)` in the compiled bundle), but the handler compared it directly against string literals (`request === "toggle-cc"`) — an array is never `===` a string, so *every* request silently fell through to `"unknown command"`. Confirmed by testing `dismiss-notif` too (same failure) before finding the array/string mismatch in `/run/user/1000/ags.js`. This means `toggle-cc` was never going to work even before the waybar-click discussion, and `dismiss-notif`/`dismiss-all` (unused by any keybind/button right now, so no visible symptom) were equally broken. Fixed by comparing `request[0]` instead. `ags-watch`'s `inotifywait` didn't pick up the fix on its own (log timestamp didn't move) — restarted AGS manually (`ags quit -i jjsanchezc-shell && ags run`) and confirmed live: `ags request -i jjsanchezc-shell toggle-cc` → `toggled`, twice (open + close).
- [x] ~~`ControlCenter.tsx`'s idle-timeout panel wrote to a dead path~~ **SUPERSEDED 2026-08-09.** Was going to fix the path (`HYPRIDLE_CONF` pointed at `/home/jjsanchezc/dotfiles-hypr/hyprland/hypridle.conf`, which doesn't exist) and did, briefly — but the whole Idle Timeouts feature (battery/charging profiles, presets, the alarm-icon header button, `ags/hypridle-profiles.json`) got removed instead, see below. Moot now.
- [x] ~~Remove Idle Timeouts panel entirely~~ **RESOLVED 2026-08-09, user's call.** Deleted from `ControlCenter.tsx`: `HYPRIDLE_CONF`/`PROFILES_FILE` constants, `IdleProfile`/`ProfileKey` types, `DEFAULT_PROFILES`, `parseIdleTimeouts`/`loadProfiles`/`saveProfiles`/`buildHypridleConf`/`applyIdleTimeouts`/`isOnAC`, the battery-state auto-switch listener, the header's alarm-icon button, and the whole `IdleTimeoutsPanel` component + its revealer. Also deleted `ags/hypridle-profiles.json` (the file it read/wrote) and its README mention. `hypr/hypridle.conf` itself is untouched — it's the one real, static config now, no more per-profile UI to fight with it.
- [x] ~~Remove WiFi from ControlCenter~~ **RESOLVED 2026-08-09, user's call.** Turned out to be more than a style choice — the system actually runs **`iwd`**, not NetworkManager (`iwd.service` active, `NetworkManager` inactive, `nmcli` not even installed), so `AstalNetwork` (assumes NetworkManager) and `connectToAp()`'s direct `nmcli` calls were never going to work here regardless. Explains why `iwgtk` (iwd's own GUI) was already wired into waybar's network click before any of this. Deleted `WifiToggle`/`WifiDetails`, the `AstalNetwork` import, `wifiExpanded` state; Bluetooth now sits alone in that row.
- [x] ~~"Reload AGS" button (Power Panel) had the same missing `-i` instance bug~~ **RESOLVED 2026-08-09.** Same root cause as the `Super+C` bug above, but harder to hit: `ags quit; ags run` was self-referential — it ran *from inside* the AGS process it was about to kill, so once `ags quit` (still missing `-i` on the first pass) fixed and started killing the right instance, the surviving shell script itself sometimes died alongside its parent before reaching `ags run`. Confirmed live: user clicked it, AGS never came back, `ags list` empty. Fixed by wrapping in `setsid` (`setsid bash -c 'ags quit -i jjsanchezc-shell; sleep 1; cd ~/.config/ags && exec ags run' &`) so the restart survives its own parent dying — verified by running the exact bound command directly: AGS came back, `toggle-cc` still worked after.
- [ ] **Note, not yet acted on**: while chasing the above, saw two `ags-watch` processes at once (one parented under the other) right after an earlier `hyprctl reload`. Re-ran a clean before/after process-count test around a fresh `hyprctl reload` afterward and it did **not** reproduce (waybar count stayed at 1) — logging this as a one-off oddity to watch for, not a confirmed reproducible bug. If it recurs, check whether `hl.on("hyprland.start", ...)` is somehow re-firing on reload.
- [x] ~~`ags-watch`'s `inotifywait` isn't reliably catching every file edit~~ **RESOLVED 2026-08-09, user's call — dropped the broad watch instead of debugging it further.** Kept restarting AGS by hand all session because the multi-file, multi-extension recursive watch (`.ts`/`.tsx`/`.scss`/`.css` across all of `ags/`) kept missing edits; started diagnosing it (a live `inotifywait -m` test on a plain `echo >>` write showed a normal `MODIFY`+`CLOSE_WRITE`, so the exact failure mode was still unconfirmed) but the user opted to just drop that loop rather than keep chasing it. `ags-watch` now only starts AGS + fixes the boot wallpaper race — no dev hot-reload; restart AGS by hand after editing code (`ags quit -i jjsanchezc-shell; (cd ~/.config/ags && ags run &)`).
  Kept a second, much narrower watch: `ags/_colors.scss` (matugen's output on every `wallpaper-set`) — needed so AGS still picks up new theme colors without a manual restart on every wallpaper change, which the broad-watch removal would otherwise have silently broken. Verified this one for real, not just assumed reliable: a live `inotifywait -m` test showed matugen writes it with a plain `MODIFY`+`CLOSE_WRITE` (no rename tricks), and an actual `wallpaper-set` run restarted AGS automatically (`gjs` PID changed, `ags-watch.log` logged a fresh `AGS started`, `toggle-cc` still worked after).
- [x] ~~README waybar/ControlCenter sections stale~~ **RESOLVED 2026-08-09.** Module layout and the `toggle-cc` disconnect above are now documented.
- [x] ~~README missing `bash/`, `starship/`, `hyprconf2lua`~~ **RESOLVED 2026-08-09.** All three added to the Directory Overview / scripts table.
- [x] ~~Stray binary: `scripts/.local/bin/claude`~~ **RESOLVED 2026-08-09 — gitignored, not deleted.** First tried `git rm` — it came right back, untracked, within the same session: `~/.local/bin` *is* `scripts/.local/bin/` (whole-folder symlink), and Claude Code self-heals this exact symlink whenever it's missing (confirmed live: deleted it, `which claude` still resolved seconds later to a fresh copy). So it's not a stray dotfile to delete, it's a machine-managed launcher that happens to live inside the repo's folder structure — same category as `monitors.lua` ("excluded from git, machine-specific"). Added to `.gitignore`, untracked via `git rm --cached` (kept the file on disk). `which claude` still resolves.
- [x] ~~`backup_hyprland/` doesn't match its own README~~ **RESOLVED 2026-08-09, user's call.** `git rm -r`'d the duplicate `.lua` set and the ~25MB `Wallpapers/` copy — down to the 9 `.conf` files + `README.md`, matching what the folder's own README already claimed it was for.
- [x] ~~`tmux/colors.conf` isn't version-controlled~~ **RESOLVED 2026-08-09 — bundled with a bigger bug found in the same investigation.** Two separate things, both fixed:
  1. **The versioning gap** — `~/.config/tmux/` was a real directory (only `tmux.conf` individually symlinked), unlike every other app's whole-folder symlink. Copied the live `colors.conf` and `plugins/` into `tmux/`, deleted the real directory, replaced it with `~/.config/tmux → Dotfiles/tmux` (matching everything else). `colors.conf` is now tracked like every other app's matugen output; `plugins/` stays gitignored (`.gitignore` already had `tmux/plugins` waiting, half-set-up from before).
  2. **Bigger bug this surfaced**: `tmux.conf` never had a `source-file colors.conf` line at all — tmux's theme had *never* actually followed the wallpaper, version-controlled or not. Added it, but a naive placement near the top got immediately overridden: the `catppuccin/tmux`/`tmux-dotbar` plugins set their own status/border styles when TPM loads them later in the same file, and tmux applies `set` commands "last one wins." Confirmed this happened live (`tmux show-options -g status-style` showed catppuccin's colors, not matugen's, right after a clean reload) before moving the `source-file` line to *after* the TPM `run` line — reloaded again, values matched `colors.conf` exactly that time.
  Deferred: the duplicate TPM install found in the same investigation (`~/.tmux/plugins/` — the one `tmux.conf`'s `run` line actually uses — vs. `~/.config/tmux/plugins/`, a second full copy that isn't referenced anywhere obvious). User explicitly scoped this round to the two items above; consolidating TPM is its own decision for later.
- [x] ~~`wl-clipboard` not installed~~ **RESOLVED 2026-08-09.** Installed `wl-clipboard` (official repo). Also found `cliphist` itself wasn't installed either (official `extra` repo) — installed that too, or `Super+Shift+V` would've stayed broken even with `wl-copy` present. The two `wl-paste --watch cliphist store` autostart lines had failed silently at login (packages didn't exist yet) — started both live (`pgrep` confirms 2 processes) instead of waiting for next login. Verified end-to-end: `echo test | wl-copy` → shows up in `cliphist list` immediately. Then found a **third** missing piece while re-checking `region-capture`'s `required_cmds`: `wayfreeze` (AUR, not official repos) — installed via `yay -S wayfreeze`. All 5 deps (`grim`/`wl-copy`/`slurp`/`wayfreeze`/`notify-send`) now confirmed present; `full-capture` and `region-capture` are both unblocked, not just the clipboard-history bind.
- [x] ~~`Super+V` is bound twice~~ **RESOLVED 2026-08-09.** `Super+V` stays `window.float` toggle; the `cliphist` picker moved to `Super+Shift+V`. Verified no existing bind used that combo, `hyprctl --verify-config` → ok, reloaded live.
- [x] ~~`swayosd` decision~~ **RESOLVED 2026-08-09 — dropped.** Turned out to be a pure cosmetic gap, not a functional one: volume/brightness are already controlled for real by `wpctl`/`pamixer` in `keybindings.lua`, independent of swayosd — the package would only have added the on-screen popup feedback, nothing else. Not worth installing for that alone. Deleted `swayosd/` and every reference to it in both READMEs.
- [x] ~~`hypr-monitor-manager` decision~~ **RESOLVED 2026-08-09 — dropped.** Traced its origin to the repo's very first commit (`bcd2f09f`) — it was a planned custom script (`auto`/`lid` modes, bound to `Super+P`/`Super+Shift+P`) never actually written. Both keybinds were dropped somewhere along the way (`Super+P` is now pseudo-tile, unrelated); only a dangling README row survived. Removed from the README's Startup Applications table.
- [x] ~~Full dormant-directory audit~~ **DONE 2026-08-09.** Confirmed via `pacman -Qi`/`which`/live `~/.config` symlinks which repo directories are actually wired up vs. sitting there unused. Both READMEs now say so per-directory instead of implying everything's live:
  - **Package not installed, nothing symlinked** (already known: `foot/`; newly confirmed: `lf/`, `wlogout/`, the latter fully superseded by `power-menu` — deleted entirely 2026-08-09, see below).
  - **Never deployed on this system**: `fonts/` (`~/.fonts`, `~/.config/fontconfig` don't exist), `themes/` (`~/.themes`, `~/.icons` don't exist), `default-apps/` (`~/.config/mimeapps.list` is a real untracked file, not a symlink to this repo's copy).
  - **`Key Dependencies` in README was aspirational, not actual** — Notifications said Mako (not installed; AGS is real), Terminal listed Foot as if equal to Kitty, File manager listed lf, Screenshots listed wl-clipboard (see item above), Tools listed fzf/zoxide/eza/bat/cliphist/lazygit (none installed). Split into an "actually active" list and a "referenced but not installed" list.
  - `nixos/` was **not** touched — it already honestly says "not used on Arch, kept for reference," no drift there.
  - Also documented two previously-unmentioned backup folders in the same "reference only" category as `backup_hyprland/`: `kitty_backup/`, `nvim_backup/`.

## 3. Recommended fixes (priority order)

**All items originally listed here are resolved as of 2026-08-09 — see §1/§2 for hypridle, ControlCenter's trigger, and the wl-clipboard/cliphist/wayfreeze chain.** Nothing currently queued; pull from §2's remaining unchecked items or §4 when picking up more work.

## 4. Recommended additions

- [x] ~~Extend matugen templates to `starship.toml`~~ **RESOLVED 2026-08-09 — declined, see §2.**
- [x] ~~`wlogout/` decision~~ **RESOLVED 2026-08-09 — dropped.** Never installed, fully superseded by `power-menu` (same 5 actions, actually bound to `Super+Shift+X`). Not worth theming a dead directory. Deleted `wlogout/` entirely and every reference to it in both READMEs.
- [x] ~~`systemctl --user enable --now battery-alert.timer`~~ **RESOLVED 2026-08-09** — see §1.
- [x] ~~Verify `catppuccin/` end-to-end~~ **RESOLVED 2026-08-06.**
