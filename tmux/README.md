# Configuration
First of all:
1. Install and configure: [TPM](https://github.com/tmux-plugins/tpm/blob/master/README.md)
2. nvim `~/.config/tmux/tmux.conf`
3. prefix + I (Uppercase I). To install the plugins

